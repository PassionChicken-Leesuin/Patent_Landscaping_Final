# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# Secrets
.env
*.key

# OS
.DS_Store
Thumbs.db

# Large/model artifacts
*.ckpt
*.pt
*.bin
*.safetensors
outputs/
checkpoints/
wandb/

# MAS run outputs (regenerated by scripts/run_mas.py). Keep only the ranked label files
# (flat self-driving + per-domain subdirs), which Colab needs for the MAS downstream arm.
DataSet/mas/*
!DataSet/mas/mas_ranked_scores.csv
!DataSet/mas/*/
!DataSet/mas/*/mas_ranked_scores.csv
DataSet/mas/*/mas_audit.jsonl
DataSet/mas/*/run_*.log

# Expanded-pool raw intermediate (regenerable from USPTO bulk via collect_*.py)
DataSet/expanded/

# Regenerable in Colab via scripts.build_candidate_all / run_snorkel (flat + per-domain)
DataSet/processed/candidate_all.csv
DataSet/processed/snorkel_labeled_all.csv
DataSet/processed/*/candidate_all.csv
DataSet/processed/*/snorkel_labeled_all.csv

# Presentation files
*.pptx
