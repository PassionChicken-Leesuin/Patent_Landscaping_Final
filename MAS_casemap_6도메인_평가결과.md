# 사례매핑 MAS 파이프라인 — 6개 골드 도메인 평가 결과 (2026-07-25)

Bergeaud 6개 기술 도메인 골드셋에서 **새 사례매핑 파이프라인(casemap)** 을 자율(`--hitl off`) 실행하고, 기존 파이프라인 최종본(full5)과 **동일 도메인·동일 eval셋**에서 대조했다. 판정 기준: `scripts/eval_agentic.py` → gold 라벨 대비 macro-F1 / AUC / recall / precision.

- 실행: `python -m scripts.eval_agentic --domains all --hitl off --variant casemap`
- 새 파이프라인: 정합진단 → 설계안(Tier) → 사례매핑(자기수정) → 결정카드 → 기준서(validator 루프) → 전수 판정. 소유자 문서 없음 → 웹 리서치 폴백.
- 비교 대상 full5: 前단계 재설계 이전의 기존 agentic 시스템 최종본.

## 도메인별 신(casemap) vs 구(full5)

| 도메인 | macro-F1 (신) | macro-F1 (구) | ΔF1 | AUC 신/구 | recall 신/구 | precision 신/구 |
|---|---|---|---|---|---|---|
| 자율주행 | **0.768** | 0.759 | **+0.009** | 0.879 / 0.856 | 0.735 / 0.674 | 0.613 / 0.624 |
| 3D프린팅 | **0.903** | 0.885 | **+0.018** | 0.963 / 0.954 | 0.961 / 0.961 | 0.780 / 0.741 |
| 블록체인 | 0.874 | 0.905 | −0.031 | 0.961 / 0.974 | 0.969 / 0.976 | 0.675 / 0.744 |
| 컴퓨터비전 | 0.808 | 0.855 | −0.046 | 0.892 / 0.918 | 0.641 / 0.824 | 0.772 / 0.745 |
| 유전자편집 | 0.867 | 0.919 | −0.053 | 0.962 / 0.967 | 0.716 / 0.855 | 0.935 / 0.928 |
| 수소저장 | 0.749 | 0.770 | −0.021 | 0.807 / 0.897 | 0.498 / 0.547 | 0.761 / 0.766 |
| **평균(6)** | **0.828** | **0.849** | **−0.021** | 0.911 / 0.928 | 0.753 / 0.806 | 0.756 / 0.758 |
| 5도메인평균(자율주행 제외) | 0.840 | 0.867 | −0.026 | — | — | — |

## 정직한 해석

- **전체적으로는 소폭 하락.** 평균 macro-F1 0.849 → **0.828 (−0.021)**. 6개 중 **2개(자율주행·3D프린팅)에서 이기고 4개에서 졌다.**
- **원인은 recall 하락.** 진 4개 도메인에서 precision은 비슷하거나 높은데 recall이 떨어졌다(컴퓨터비전 0.824→0.641, 유전자편집 0.855→0.716, 수소저장 0.547→0.498). 새 前단계의 **날카로운 제외 기준·E-tier 커버리지 강제**가 정밀도를 지키는 대신 일부 도메인에서 유효특허를 과잉 제외한 것. 이는 휴머노이드 골드 구축에서 관측한 recall↔precision 트레이드오프와 동일한 현상이다.
- **precision·AUC는 대등.** 평균 precision 0.758→0.756(동일), AUC 0.928→0.911(−0.017).
- **성능만 보면 개선이 아니라 "대등~소폭 하락"이다.** 새 파이프라인의 가치는 성능 우위가 아니라 **해석가능성·감사가능성·재현성**(사례 매핑 표, 명시적 경계 결정 카드, 규칙 12개로 코드화된 기준서)에 있다. 즉 "성능을 거의 유지하면서 판정 과정을 사람이 검토·수정 가능하게 만든 것"이 기여점.

## 시사점 (개선 방향)

- **도메인별 제외 aggressiveness 편차가 큼.** 컴퓨터비전·유전자편집처럼 recall이 크게 빠진 도메인은 E-tier가 과하게 잡았을 가능성 — 도메인별 제외 강도 자동 조절(또는 리콜 편향 유지)이 필요.
- 자율주행·3D프린팅에서는 오히려 개선 — 前단계가 look-alike가 많은 도메인(자율주행: cruise control 등)에서 경계를 더 잘 그음.
- 결론: **성능 중립적 재설계 + 해석가능성 획득.** 기술경영 관점 논문화 시 "성능 손해 없이(≈−0.02 F1) 재현·감사 가능한 유효특허 선별 절차를 얻었다"로 프레이밍하는 것이 정직하고 방어 가능하다.

## 산출물

- 도메인별 원지표: `outputs/agentic_metrics_<domain>_casemap.json` (신) / `_full5.json` (구)
- 종합 비교 CSV: `outputs/agentic_comparison_casemap.csv`
- 각 도메인 판정 워크스페이스: `DataSet/agentic/<domain>-casemap/` (기준서·판정·사례매핑·결정카드)

*주: `outputs/`는 .gitignore 대상이라 원지표 JSON은 리포에 포함되지 않는다. 위 표가 결과의 정본이다.*
