# ── Local stack: data pipeline + MAS labeling. Verified on Python 3.14 (.venv) ──
pandas>=2.0
numpy>=1.24
openai>=2.0          # MAS structured-output client (chat.completions.parse)
python-dotenv>=1.0   # load 10 OPENAI_API_KEY_1..10 from .env
pydantic>=2.6        # structured-output schemas
langgraph>=0.2       # optional spec-faithful graph wrapper (src/mas/graph.py)

# ── Snorkel baseline arm (run in Colab/Kaggle, Python 3.10/3.11) ──
# snorkel 0.10 does NOT support Python 3.14 — use a 3.10/3.11 env.
# snorkel==0.10.0

# ── SciBERT fine-tuning + evaluation (run on GPU: Colab T4 / Kaggle) ──
# torch>=2.2
# transformers>=4.40
# datasets>=2.18
# scikit-learn>=1.3
# accelerate>=0.30

# ── MAS arm (LangGraph) ──
# langgraph>=0.2
# langchain-core>=0.3
# pydantic>=2.6
# python-dotenv>=1.0
# one provider sdk, e.g.:
# langchain-openai   /   langchain-anthropic   /   langchain-google-genai
